{
  "name": "sote",
  "private": true,
  "version": "0.0.1-dev",
  "license": "AGPL-3.0-only",
  "packageManager": "pnpm@9.15.0",
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "build:core": "pnpm --filter @sote/core build",
    "build": "pnpm build:core && pnpm --filter @sote/server build && pnpm --filter @sote/web build",
    "typecheck": "pnpm build:core && pnpm -r typecheck",
    "test": "pnpm build:core && pnpm -r test",
    "dev": "pnpm --filter @sote/web dev",
    "check": "node scripts/check-migrations.mjs && node scripts/check-task-columns.mjs && node scripts/check-env-passed.mjs && node scripts/check-routes-reachable.mjs && node scripts/check-styles.mjs && node scripts/check-css-dupes.mjs && node scripts/check-assets.mjs && node scripts/check-data-hooks.mjs && node scripts/check-accent-follows.mjs && node scripts/check-hook-order.mjs && pnpm typecheck && pnpm test"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}
